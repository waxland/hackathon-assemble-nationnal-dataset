version: '3.8'

services:
  france2030-dashboard:
    build: .
    container_name: france2030-dashboard
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
